openapi: '3.0.0'
info:
  title: signals contract
  version: '1.0.0'
paths:
  /signals/user/{user_id}:
    get:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
