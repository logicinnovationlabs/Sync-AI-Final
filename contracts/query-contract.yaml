openapi: '3.0.0'
info:
  title: query contract
  version: '1.0.0'
paths:
  /api/v1/search:
    post:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
