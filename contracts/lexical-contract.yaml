openapi: '3.0.0'
info:
  title: lexical contract
  version: '1.0.0'
paths:
  /search/lexical:
    post:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
